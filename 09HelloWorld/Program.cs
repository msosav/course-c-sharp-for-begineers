using System; // This imports the System namespace, which contains the Console class

namespace HelloWorld // This is the namespace declaration, a namespace is a way to organize your code, and you are allowed to define your classes in a namespace
{
  class Program // This is a class declaration, the class Program contains the data and method definitions that your program uses
  {
    static void Main(string[] args) // This is a method declaration, the Main method is the entry point of a C# application
    {
      Console.WriteLine("Hello World!");
    }
  }
}
